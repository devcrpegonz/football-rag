# football-rag

Work in progress. RAG system for football training sessions.
Upload training sessions and query them with natural language.

Stack: Laravel 13 + PostgreSQL 16 + pgvector + OpenAI + Redis + Docker

Currently in early development
